apiVersion: v1
name: "go-std"
description: "Go Standard Repo"
version: "0.1.0"
tags: ["go-std"]

variables:
  - name: project_name
    description: "Name of the project"
    type: string
    required: true
