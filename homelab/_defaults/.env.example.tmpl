# 1Password Developer Environment file for ${project_name}.
# Resolved at command time via `op run --env-file=.env -- <cmd>`.
# https://developer.1password.com/docs/environments/local-env-file
#
# OP_SERVICE_ACCOUNT_TOKEN must be a literal value (not op:// reference).
OP_SERVICE_ACCOUNT_TOKEN=ops_xxxxxxxxxxxxxxxxxxxxxxxx

# TODO: replace below with op:// references for the secrets this repo needs.
# Example:
# AWS_ACCESS_KEY_ID=op://Homelab/garage/AWS_ACCESS_KEY_ID
# AWS_SECRET_ACCESS_KEY=op://Homelab/garage/AWS_SECRET_ACCESS_KEY
